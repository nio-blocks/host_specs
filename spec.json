{
  "nio/HostSpecs": {
    "version": "0.1.0",
    "description": "Get general specifications of the nio instance host.",
    "properties": {
      "menu": {
        "title": "Menu",
        "type": "ObjectType",
        "description": "Flags for turning off/on various specs.",
        "default": {
          "python": false,
          "processor": false,
          "platform": false,
          "machine": false,
          "dist": false,
          "system": true,
          "os_version": false,
          "hostname": false
        }
      }
    },
    "inputs": {
      "default": {
        "description": "Any list of signals"
      }
    },
    "outputs": {
      "default": {
        "description": "An attribute is added for each specification read. Attribute names are the menu name followed by an underscore and then then specific specification."
      }
    },
    "commands": {}
  }
}
